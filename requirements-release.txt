# Exact top-level tools used by the write-capable release job.
# Dependabot maintains these pins; PIP_CONSTRAINT also applies setuptools
# to the isolated package-build environment.
pip==26.2.1
build==1.5.0
twine==7.0.0
setuptools==84.0.0
wheel==0.48.0
